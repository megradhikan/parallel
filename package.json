{
  "name": "parallel",
  "private": true,
  "version": "0.1.0",
  "description": "Real-time collaborative code editor: Monaco with a hand-rolled Yjs CRDT binding, Redis pub/sub fanout, and two streamed Claude features.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/megradhikan/parallel.git"
  },
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "typecheck": "pnpm --filter backend typecheck && pnpm --filter frontend typecheck",
    "test:concurrent": "pnpm --filter backend test:concurrent",
    "test:cross-instance": "pnpm --filter backend test:cross-instance",
    "test:load": "pnpm --filter backend test:load"
  }
}
